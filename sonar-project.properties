# SonarCloud configuration
# Bilingual content (es/en) and the generated published JSON legitimately mirror
# the same structure across locales, so the copy-paste detector flags them as
# duplicated. Exclude the content data from duplication detection.
sonar.cpd.exclusions=src/content/seed/**,src/content/published/**
